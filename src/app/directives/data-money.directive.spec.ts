import { DataMoneyDirective } from './data-money.directive';

describe('DataMoneyDirective', () => {
  it('should create an instance', () => {
    const directive = new DataMoneyDirective();
    expect(directive).toBeTruthy();
  });
});
