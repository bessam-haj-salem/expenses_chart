import { DataChartDirective } from './data-chart.directive';

describe('DataChartDirective', () => {
  it('should create an instance', () => {
    const directive = new DataChartDirective();
    expect(directive).toBeTruthy();
  });
});
